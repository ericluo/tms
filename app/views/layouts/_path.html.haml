- if @breadcrumbs.present?
  %ul.breadcrumb
    - @breadcrumbs[0..-2].each do |crumb|
      %li
        = link_to crumb[:name], crumb[:url], crumb[:options]
        %span.divider= >
    %li.active
      = link_to @breadcrumbs.last[:name], @breadcrumbs[:url], @breadcrumbs[:options]
